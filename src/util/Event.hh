#ifndef UTIL_EVENT
#define UTIL_EVENT

// Event is such a thing that drives the system to move.
// If no event is triggered, all non-constant changes won't occur.

enum EventType
{

};

class Event
{
};

#endif /* UTIL_EVENT */
